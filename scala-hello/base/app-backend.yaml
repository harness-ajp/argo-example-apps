---
# Backend Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: scala-backend
  labels:
    app: scala-hello-world
    component: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scala-hello-world
      component: backend
  template:
    metadata:
      labels:
        app: scala-hello-world
        component: backend
    spec:
      containers:
      - name: backend
        image: docker.io/ajpahl1008/scala-hello-world-backend:11
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        env:
        - name: PORT
          value: "8080"
        - name: JAVA_OPTS
          value: "-Xmx512m -Xms256m"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/status
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
---
# Backend Service
apiVersion: v1
kind: Service
metadata:
  name: scala-backend
  labels:
    app: scala-hello-world
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: scala-hello-world
    component: backend
