apiVersion: apps/v1
kind: Deployment
metadata:
  name: ngnix
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ngnix
  template:
    metadata:
      labels:
        app: ngnix
    spec:
      containers:
      - name: ngnix
        image: docker.io/library/ngnix:1.25
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: ngnix
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: ngnix
